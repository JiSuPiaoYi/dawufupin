<view class="page">
    <view class="page__bd">
        <view style='position:fixed;top:0;width:100vw;'>
            <progress percent="{{progress_percent}}" stroke-width="3" />
        </view>
        <image src='/images/loading_.jpg'></image>

        <!-- <view class='modal-shade' wx:if="{{!hasUserInfo && canIUse}}">
            <button class="modal-btn" type="primary" open-type="getUserInfo" bindgetuserinfo="getUserInfo">微信登录</button>
        </view> -->

        <button class="login-btn" type="primary" open-type="getUserInfo" bindgetuserinfo="getUserInfo" wx:if="{{!hasUserInfo && canIUse}}">微信登录</button>
        
    </view>
</view>


